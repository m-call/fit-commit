<nav class="navbar">
    <a href={{profileUrl user.id}}>Profile</a>
    <a href='/friends'{{friendsUrl user.id}}>Friends</a>
    <a href='/leaderboard'>Leaderboard</a>
</nav>

<h1>Leaderboard</h1>
{{#each leaders as |user| }}
    <div>
        <span>{{user.name}}</span>
        <span>{{user.score}}</span>
    </div>
{{/each}}

